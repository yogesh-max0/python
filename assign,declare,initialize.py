name = 'yogi'
age = 22
salary = 75000

name,age,salary,rating = 'yogesh',24,100000,4.9
print(name,rating,age,salary,sep='....')


name = 'yogi'
dob = '29-12-2003'

'''
------------------------------------------------------------
'''

name,age = '29-12-2003','eligible'
print(type(name))
print(type(age))
print(name,age)

'''
---we can asssign like this in assignment and even we can change the values
like how we newly assign so that can print the newly assigned values
'''


# DECLARATION
x = int(input()) # declared ---we cant fix any other values because already we given the value type
x = 5                # Automatically declared as an integer (int)
name = "yogi"       # Automatically declared as a string (str)
pi = 3.14            # Automatically declared as a floating-point number (float)
is_active = True     # Automatically declared as a boolean (bool)

#INTITIALIZATION
age = 20            #initialized this is already finalized value
name = 'yogi'       #initialized

